<h1>Burgers</h1>

<div>
  <h2>All the Burgers you want to eat</h2>

  <ul>
    {{#each burgers}}
    <p>Shows the number of burgers in data</p>
    {{!-- {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/unless}} --}}
    {{/each}}
  </ul>
  {{!-- <ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul> --}}
</div>
<h2>All the Burgers you have devoured:</h2>

<ul>
  {{#each burgers}}
  {{!-- {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}} --}}
  {{/each}}
</ul>
{{!-- <ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2>Create a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Devoured?</label><br>


    <input type="radio" name="sleepy" value="1" checked> Eat Now!<br>
    <input type="radio" name="sleepy" value="0"> Devour Later!
    {{!-- Original - CAT --}}
    {{!-- <input type="radio" name="sleepy" value="1" checked> Eat Now!<br>
    <input type="radio" name="sleepy" value="0"> Devour Later! --}}
  </div>

  <button type="submit">Add Burger</button>
</form>